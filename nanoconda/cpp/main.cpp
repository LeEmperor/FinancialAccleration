#include <iostream>
#include <string>
#include <algorithm>
#include <set>
#include <unordered_map>
#include <vector>
using namespace std;

#define nL "\n"
// #define VI vector<int64_t>
// #define VII vector<VI>
#define VI vector<int>
#define VVI vector<VI>
#define VL vector<long>
#define VLL vector<VL>
#define VS vector<string>
#define VVS vector<VS>
#define pb(a) push_back(a)
#define sz size()
#define bloon bool
#define no false
#define ye true

#define MII map<i64, i64>
#define MCI map<char, i64>

#define u8 uint8_t
#define i8 int8_t
#define u16 uint16_t
#define i16 int16_t
#define u32 uint32_t
#define i32 int32_t
#define u64 uint64_t
#define i64 int64_t

#define pvec(vec)            \
    do {                           \
        for (int x : (vec)) {      \
            std::cout << x << ' '; \
        }                          \
        std::cout << '\n';         \
    } while (0)

#define f_i(i, c) \
    for (size_t i = 0, _n = (c).size(); i < _n; ++i)


int main() {

  return 0;
}


